node_modules
dist
dist-ssr
*.local
.vite

# Generated dev saves (npm run seed-save)
*.civsave

# Profiler output (npm run sim:profile) — flame writes into the CWD
cpu-profile-*
heap-profile-*

# Bundled dev scripts (rebuilt on every npm run by scripts/bundle.mjs)
scripts/.bundle/

# Editor / OS
.DS_Store
.idea
*.log

# Personal notes (not tracked)
personal/
